{
  "name": "rencoder.provid.gg",
  "version": "1.1.0",
  "description": "Remote Encoder — Free, self-hosted encoder remote by PROVID",
  "type": "module",
  "author": "Nico Laeser / PROVID",
  "private": true,
  "scripts": {
    "build:ts": "tsc",
    "build:css": "node_modules/.bin/tailwindcss -i tailwind.input.css -o public/css/tailwind.css --minify",
    "build:inject": "node scripts/inject-domain.mjs",
    "build": "npm run build:inject && npm run build:ts && npm run build:css",
    "dev": "node scripts/inject-domain.mjs && concurrently \"tsc --watch\" \"node_modules/.bin/tailwindcss -i tailwind.input.css -o public/css/tailwind.css --watch\" \"node --watch dist/server.js\"",
    "start": "node dist/server.js",
    "prettier": "npx prettier --write \"src/**/*.{js,jsx,ts,tsx,css,scss,json,md,yml,yaml}\" \"public/**/*.{html,js,css,json}\"",
    "cu": "npx npm-check-updates -u"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.2",
    "@types/ws": "^8.18.1",
    "concurrently": "^10.0.3",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3"
  }
}
